name: actionpage
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: heloo
        run: 
             echo "HI raghu"
             echo "hi github"

  test:
   runs-on: ubuntu-latest
   steps:
     - name: test phase
       run: echo "hi rakku"

  deploy:
   runs-on: ubuntu-latest
   steps:
     - name: deploy phase
       run: echo "deploy phase"
   
    
     
    
  
